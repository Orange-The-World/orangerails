#!/usr/bin/env bash

set -uo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel 2>/dev/null)"
if [[ -z "$REPO_ROOT" ]]; then
  printf "commit-msg: cannot locate the repository; refusing without scanning\n" >&2
  exit 2
fi

exec bash "$REPO_ROOT/scripts/pre-publish-scan.sh" --commit-message "$1"
