import { PageScaffold } from "./_shared";

export default function Apps() {
return (
    <PageScaffold
      title="Apps"
      description="Apps within your org. Each app has its own API keys and quota."
    />
  );
}
