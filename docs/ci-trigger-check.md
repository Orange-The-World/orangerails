# CI trigger check

Scratch file. It exists only so a pull request can be opened whose base is a
feature branch rather than `dev` or `prod`, to confirm that the secret scan and
the pre-publish hygiene scan now run on that shape of pull request.

This file is never merged. The pull request that carries it is closed once the
check list has been recorded.
