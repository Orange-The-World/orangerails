# Scratch file, OR-T1102 proof PR

This file exists only to give this throwaway pull request a diff. The PR is
never merged: it exists to watch `prod-pr-source-guard.yml` reject a pull
request whose head is not `dev` and not `release/*`, with the run link
recorded on OR-T1102. Safe to delete once that proof is captured.
