-- OR-T1189 forced-red proof, step 7. Scratch test file, not a real migration.
-- Part of a two-PR race test: this file alone does not collide with anything
-- on dev at the time this branch was cut. Reverted in the same session
-- immediately after the run is observed.
select 1;
