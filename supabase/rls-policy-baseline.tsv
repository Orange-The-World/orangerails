# SUPERSEDED. This file is read by nothing.
#
# The RLS policy baseline is now per cluster, because dev and prod do not
# carry the same policy set (67 and 73 policies in schema public when this
# was written). The deploy workflow selects the file from the resolved
# target:
#
#   supabase/rls-policy-baseline.dev.tsv    dev
#   supabase/rls-policy-baseline.prod.tsv   prod
#
# Do not regenerate this path and do not point the check at it. It remains
# only because the tooling that produced this commit cannot delete a file;
# it should be removed with: git rm supabase/rls-policy-baseline.tsv
